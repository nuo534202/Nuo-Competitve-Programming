#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios::sync_with_stdio(false); cin.tie(0); cout.tie(0);
    int x;
    cin >> x;
    cout << "Today, I ate " << x << " apple" << (x > 1 ? "s." : ".") << endl;
    return 0;
}